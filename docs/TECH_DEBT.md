# Tech-debt & gripes ledger

Living record of known drift, debt, and "someone should fix this" items, fed by
the **Coffee Break / Standup protocol** (see `.claude/AGENT_PROTOCOL.md`).
Kiroku curates it; Adam ranks by risk. Each item: severity, what, where, owner,
date noticed. Remove items when resolved (note the fix in the git history, not here).

Severity: **BLOCKER** (unsafe / broken) > **RISK** (latent bug / safety-adjacent) >
**ANNOYANCE** (friction, papercut) > **NIT** (cosmetic).

| Sev | Item | Where | Owner | Noticed |
|---|---|---|---|---|
| RISK | `tct_gui.py` is trending toward a God object (~870 lines: panels, wiring, connect/scan orchestration, shutdown). Extract ConnectionController / ScanCoordinator / shutdown unit. | `TCT_app/tct_gui.py` | Noah | 2026-07-06 |
| RISK | `tek_fastframe` scope backend is non-functional — vendored `dustin_scope` package missing from `TCT_app/vendor/`; targets MSO5204B, not the bench TBS1052C. | `TCT_app/devices/oscilloscope_tek_fastframe.py` | Paul | 2026-07-06 |
| RISK | Only the oscilloscope implements `is_alive()`; motor/bias/camera still use the flag-based default, so a yanked cable on those shows CONNECTED. (Bias HV probe needs a cited manual command first.) | `TCT_app/devices/*` | Paul | 2026-07-06 |
| ANNOYANCE | `_OscilloscopeSection.to_dict()` hardcodes DRS4 `trigger_source`/`time_correction`/`t0_ns` regardless of form state — no UI control. | `TCT_app/gui/settings_window.py` | Noah | 2026-07-06 |
| TODO(bench) | Confirm the iseg polarity relay settle time; the 0.5 s confirm budget (`_POL_CONFIRM_BUDGET_S`) is an unverified guess flagged in the research note. | `TCT_app/devices/bias_supply_iseg.py` | Paul | 2026-07-06 |
| RISK | Real `IsegBiasSupply.set_polarity` gate logic has no automated coverage — tests exercise only the simulated backend. Add fake-`_inst` tests for the refuse-on-Is-On / refuse-above-threshold / refuse-on-None-status / confirm-timeout paths. | `TCT_app/tests/test_bias_polarity.py` | Paul | 2026-07-06 |
| TODO(bench) | Verify `CH1:PRObe:GAIN?` and `CH1:COUPling?` query forms answer on the TBS1052C; if model-specific, guard like the WFMOutpre?/WFMPre? fallback. | `TCT_app/devices/oscilloscope.py` | Paul | 2026-07-06 |
| TODO(bench) | Verify iseg SCPI token forms on real HV: lowercase p/n accepted, `:READ:CHAN:STAT?` bit3=Is-On, `:CONF:OUTP:POL:LIST?` / `:READ:MODULE:CHANNELNUMBER?` reply formats. | `TCT_app/devices/bias_supply_iseg.py` | Paul | 2026-07-06 |
| NIT | Scope `apply_chan_config` reports success even when zero channels are enabled (nothing sent). | `TCT_app/gui/scope_panel.py` | Noah | 2026-07-06 |
| NIT | `set_output_load` formats numeric loads with `:g` → scientific notation for large ohms a Rigol may reject (low impact; GUI only offers INFinity/50). | `TCT_app/devices/waveform_generator.py` | Paul | 2026-07-06 |
| MINOR | During global ALL-OUTPUTS-OFF, per-panel Ramp/Output buttons stay enabled → a contradictory per-tab ramp can interleave. Disable per-panel controls for the duration. | `TCT_app/gui/multi_bias_panel.py` | Noah | 2026-07-06 |
| MINOR | `_ReadoutPoller` (and `_BiasPoller`) `deleteLater` is posted after the thread loop exits → poller+timer leak one per rebuild/reload. Delete explicitly after `wait()`. Pre-existing pattern. | `TCT_app/gui/bias_panel.py` | Noah | 2026-07-06 |
| MINOR | Bias+Waveform (vscan) Start button is live on non-primary bias tabs but wired only for the primary → dead control on CH1+. Hide/disable vscan on non-primary panels. | `TCT_app/gui/bias_panel.py` | Noah | 2026-07-06 |
| NIT | statusChip unlisted-state renders neutral silently: a panel setting an unlisted `state` value (e.g. "error") gets a neutral pill with no warning. Docstring already lists valid set; acceptable as-is, logged for awareness. | `TCT_app/gui/style.py` | Noah | 2026-07-06 |
| NIT | warn chip contrast on light theme: warn `#d98c17` chip text over a 0.16-alpha tint on light bg is borderline for contrast; legible at font-weight 700 but could be darkened for light theme. Non-blocking. | `TCT_app/gui/style.py` | Noah | 2026-07-06 |
| ANNOYANCE | `influx.measurement` consumed by `data/influx_writer.py:39` (default `"slow_control"`) but absent from shipped `configs/devices.yaml` — document or add to example config. | `TCT_app/data/influx_writer.py` | Paul | 2026-07-07 |
| ANNOYANCE | Nested config dicts (`output.save.*`, `charge_calibration.reference.*`, `slow_control.channels[].*`) still not typo-checked — same boundary as `motor_stage.software_limits`; candidate for recursive known-keys pass. | `TCT_app/controller/config_validator.py` | Paul | 2026-07-07 |
| RISK | Teardown doesn't JOIN the scan worker thread: GUI-side bias ramp-down can run concurrently with `_run_plan` finally `_bias_failsafe` + a later `disconnect_all()` — two threads may hit the bias driver at once (HV still ends safe). Consider joining PlanThread/ScanThread in teardown. | `TCT_app/tct_gui.py` | Adam/Abel | 2026-07-07 |
| RESOLVED 2026-07-07 | QtDangerGate stray queued `_confirm_requested` emit — Mary's completed review reproduced it as a real stray modal during teardown (BUG, not cosmetic); fixed: `_on_confirm_requested` checks closed/released under the lock before showing, +3 regression tests (stray-after-shutdown, stray-after-timeout, dialog-exception-releases-deny). | `TCT_app/gui/qt_danger_gate.py` | Noah/Adam | 2026-07-07 |
| MINOR | Planner v2 real on-screen drag gestures aren't headless-testable (only decode/mutate logic is) — manual QA needed. | `TCT_app/gui/planner_panel.py` | Noah | 2026-07-07 |
| MINOR | panel_kit gap: Card has no checkable-header variant with auto-disable-children — blocked full conversion of bias IV-scan / vscan boxes; scan_panel has the same pattern. Add if it recurs. | `TCT_app/gui/panel_kit.py` | Noah | 2026-07-07 |
| NIT | scope_measurements.py MeasurementPanel still a plain QGroupBox inside the carded scope panel — slightly inconsistent chrome; card it whenever that file is next touched. | `TCT_app/gui/scope_measurements.py` | Noah/Adam | 2026-07-07 |
| RESOLVED 2026-07-08 | PyVISA first-import Windows access-violation (ctypes DLL load off-main-thread in settings scan QThread). Fixed: main-thread `prime_pyvisa()` warm-up in `waveform_generator.py` + GUI-thread priming in `settings_window.py`; test stubs (no real bus enumeration in tests); removal of `apply_theme` allWidgets() walk that AV'd on dying widget wrappers in `style.py`. Owner Paul/Noah, +3 regression tests (stray-after-shutdown, stray-after-timeout, dialog-exception-releases-deny) for stray emit race. | `TCT_app/devices/waveform_generator.py`, `TCT_app/gui/settings_window.py`, `TCT_app/gui/style.py` | Paul/Noah | 2026-07-08 |
| TODO(bench) | DG4162 load-SCPI fix (`:OUTPut:IMPedance` → `:OUTPut:LOAD`) + `output_load: 50` are sourced from the manual but unverified on the real unit: confirm the front panel shows 50 Ω after Apply and the delivered amplitude is no longer halved. If the model rejects `:OUTPut:LOAD`, revert. See `docs/research/pdl800_trigger_wavegen_lan.md`. | `TCT_app/devices/waveform_generator.py` | Paul | 2026-07-07 |
| RESOLVED 2026-07-08 | Wavegen armed indicator — DG4000 output-STATE query sourced: `:OUTPut{ch}:STATe?` returns `ON`/`OFF` (manual-cited from DG4000 Programming Manual p.199/208); implemented in `devices/waveform_generator.py::WaveformGenerator.connect()` to query and resolve tri-state to real `True`/`False` on connect (reads as `ON`/`1`→True, `OFF`/`0`→False). Read-only on connect, safe. +17 tests. Source: `docs/research/dg4000_tbs1000c_query_forms.md` Q1a. | `TCT_app/devices/waveform_generator.py` | Paul | 2026-07-08 |
| RESOLVED 2026-07-07 | Unipolar 0→+V trigger path wired: `level_low_V`/`level_high_V` added to WaveformGenerator construction in device_manager.py, validated in config_validator.py (both-or-neither, numeric, low<high), documented in devices.yaml, +8 tests. Bipolar stays the default. | `TCT_app/controller/device_manager.py` | Abel/Adam | 2026-07-07 |
| NIT | `tct_gui._teardown_panels` doesn't call `_settings_window.shutdown()` (the VISA scan manager is covered by an `aboutToQuit` hook inside settings_window instead); add the explicit call for symmetry with `_device_manager_window.shutdown()`. | `TCT_app/tct_gui.py` | Noah/Adam | 2026-07-07 |
| RISK | FLIR Spinnaker **64-bit** SDK runtime must be installed on any real-camera machine; the x86 installer does not work with a 64-bit Python venv. Additionally, the FLIR Blackfly camera requires a **direct USB-3 port** — connecting through a USB hub fails device detection entirely. | `TCT_app/devices/camera_blackfly.py` | Paul | 2026-07-07 |
| RESOLVED 2026-07-08 | Bench network topology is now **static IP** on isolated instrument LAN (192.168.0.x): TP-Link switch at 192.168.0.1, PC Ethernet at 192.168.0.2/24 (static, no gateway), DG4162 at 192.168.0.10/24 (no DHCP/AutoIP). Documented in `docs/BENCH_SETUP.md` (LAN topology table, PC/DG4162 static-IP restore procedures, DG4162 front-panel steps, FLIR SDK/USB-3 requirements, wavegen auto-enable safety, switch-VLAN troubleshooting) + cross-links in `TCT_app/README.md`. | `docs/BENCH_SETUP.md` | Adam/Samantha | 2026-07-07 |
| TODO(bench) | Confirm the PC's instrument-NIC friendly name is still literally "Ethernet" (the static-IP restore commands in `docs/BENCH_SETUP.md` assume this; a driver reinstall can rename it to something like "Ethernet2"). | `docs/BENCH_SETUP.md` | Adam/ops | 2026-07-08 |
| TODO(bench) | Record the TP-Link managed switch's exact model + firmware version and the reset-button hold time used for the 2026-07-07 factory-reset fix (which unblocked the LAN) — currently not documented in BENCH_SETUP.md troubleshooting §5. | `docs/BENCH_SETUP.md` | Adam/ops | 2026-07-08 |
| NIT | `test_settings_window_visa_scan.py::test_construction_does_not_block_on_slow_scan` has a tight 0.5 s wall-clock budget; flaked twice on 2026-07-07 under back-to-back pytest CPU load (passes isolated, unrelated to changesets per git-stash verify). Suggested: relax budget or mark as timing-sensitive. | `TCT_app/tests/test_settings_window_visa_scan.py` | Noah/Adam | 2026-07-07 |
| RESOLVED 2026-07-11 | Panel-kit rollout batch1/batch2 test names retitled. Codex C1 lane (57e053a): batch1/batch2 tests now use current naming (camera/analysis panels migrated in batch3 on 2026-07-07). | `TCT_app/tests/` (batch1/batch2 panel-kit rollout files) | Noah | 2026-07-07 |
| RESOLVED 2026-07-11 | RepeatabilityTester now requires DangerGate. Commit 33d1664: `controller/repeatability.py` gates motion via DangerGate, `gui/calibration_panel.py` passes gate instance to `_run_repeatability()` method. CalibrationPanel builds gate before construction (no danger until user explicitly confirms). | `TCT_app/controller/repeatability.py`, `TCT_app/gui/calibration_panel.py` | Abel/Noah | 2026-07-10 |
| RESOLVED 2026-07-13 | Silent camera-frame drops (06de0dc): NEW `camera/frame_point_index` dataset (frame→point map), `n_frames_omitted` attribute counts drops. `camera/frames` never zero-padded. Frame↔point alignment now auditable. | `TCT_app/data/hdf5_writer.py` | Jonathan | 2026-07-10 |
| RESOLVED 2026-07-13 | Mono16 display truncation-wrap (f1e1712) + binning Average attempt (00abe9c): autoscale prevents uint8 alias; `set_binning()` attempts Average mode (classic BFLY Sum-only, skip-at-INFO permanent); display rescales post-binning. Closes Kaya's bench "aliasing" + "white screen at binning 2/4" observations. | `TCT_app/gui/camera_panel.py` | Noah | 2026-07-10 |
| ANNOYANCE | Hex sweep pending: `gui/motor_panel.py` 1 hex, `gui/settings_window.py` 6 hex remain on allowlist (T3 follow-up pass). Guard test `tests/test_no_inline_hex_gui.py` prevents regressions. | `TCT_app/gui/{motor_panel,settings_window}.py` | Noah | 2026-07-10 |
| NIT | `gui/calibration_panel.py` two status labels use static LIGHT tokens, no live theme refresh on toggle (deliberate: panel may not need full parity). Follow-up if panel gains other theme-responsive elements. | `TCT_app/gui/calibration_panel.py` | Noah | 2026-07-10 |
| RESOLVED 2026-07-11 | QML rail (Devices/Settings/Log/Debug chips) used a Flickable because content overflows ~1400px viewport; slice 2a compressed without Flickable via icon buttons/overflow menu (4eb7f14). | `TCT_app/gui/qml/Shell.qml` | Noah | 2026-07-11 |
| ANNOYANCE | `gui/scope_viewmodel.py:ScopeViewModel.rateText` stubbed empty; TODO drive from scope reader cadence/acquisition-mode state. | `TCT_app/gui/scope_viewmodel.py` | Noah/Abel | 2026-07-11 |
| ANNOYANCE | Per-instance view-models (ScopeViewModel, others) exposed via QML context properties (invisible to qmllint; statically unverifiable). Only Theme singleton is a true QML singleton; others are properties. Consider refactor if more emerge. | `TCT_app/gui/qml_shell.py` | Noah | 2026-07-11 |
| RISK | Slice-1 REAL-DISPLAY bench-gate open (glass/MultiEffect look, chrome + Motor GLViewWidget coexistence under OpenGL pin, detach GPU-context rebuild, repeated Settings-save soft-reload in QML mode, Theme NOTIFY overhead). | `TCT_app/gui/qml/Shell.qml`, `TCT_app/gui/qml_shell.py` | Noah/Adam | 2026-07-11 |
| RISK | `gui/qml_shell.py:_collect_shell_state()` reads widget internals (StatusChip property/state, scope _btn_live) — degradation-only coupling; renaming those properties would silently blank the QML readout. Refactor to explicit state-reader interface. | `TCT_app/gui/qml_shell.py` | Noah | 2026-07-11 |
| RESOLVED 2026-07-10 | Full-suite flake entry (pyqtgraph grab() accumulation past ~450 GUI tests) — **FIXED** by tests/conftest.py autouse fixture draining Qt DeferredDelete after every test (T6). Suite now 690 passed clean. | `TCT_app/tests/conftest.py` | Noah | 2026-07-10 |
| RISK | ScanViewerPanel status chip always renders green "Finished" after abort or error terminal states (coordinator scan_finished is terminal-agnostic). Fix: use status-carrying finish signal or read AppState for terminal reason. | `TCT_app/gui/scan_viewer_panel.py` | Abel/Noah | 2026-07-10 |
| RISK | Voltage-scan runs fire unconditional scan_finished → viewer chip "Finished" + Open-in-Analysis enables with a voltage-scan HDF5 path the viewer never mapped; usability inconsistency. | `TCT_app/controller/scan_controller.py` | Abel | 2026-07-10 |
| TODO(archive) | `controller/plan_from_config.plan_from_scan_config` has no production caller as of S2c (ScanPanel retired); kept for API stability. Decide: remove or re-home. | `TCT_app/controller/plan_from_config.py` | Abel | 2026-07-10 |
| RESOLVED 2026-07-11 | Config-persist paths rewrote `configs/devices.yaml` via `yaml.safe_load→yaml.dump` round-trip which **stripped all YAML comments**; fixed by c86e21a: new `controller/yaml_persist.py` (comment-preserving line-level surgical editor; scope_panel/settings_window route through `merge_yaml_text` / `update_yaml_file` instead of raw dump). | `TCT_app/gui/{scope_panel,settings_window}.py`, `TCT_app/controller/yaml_persist.py` | Noah | 2026-07-10 |
| RESOLVED 2026-07-11 | Planner estimate off-thread. Commit 7903ffe: last sync `estimate_plan()` call sites (drag delta-preview + ungated `_safe_estimate`) now off GUI thread via persistent `_EstimateWorker` (QObject on dedicated QThread). `_CandidatePreview` structural counts for large drag candidates. Watchdog test `tests/test_gui_thread_watchdog.py` covers full path. | `TCT_app/gui/planner_panel.py` | Noah/Abel | 2026-07-11 |
| RESOLVED 2026-07-11 | Plan estimate/compile parity tested. Commit adff735: `tests/test_plan_parity.py` property tests — estimate_plan output cost == compiled plan step count (bias/move/settle emission rules). Shared leaf walker guarantees structural agreement. | `TCT_app/controller/plan_estimate.py`, `TCT_app/controller/plan_compiler.py`, `TCT_app/tests/test_plan_parity.py` | Abel | 2026-07-11 |
| RISK | **Coffee retro (Abel):** no queryable run-state facade over `scan_controller` (progress/state are push callbacks + threading.Events) — QML MVVM needs a bindable run-state view-model (step index/total, run-state enum, pause/resume/abort). Design before the shell's Scan Viewer slice needs it. | `TCT_app/controller/scan_controller.py` | Abel | 2026-07-11 |
| RESOLVED 2026-07-11 | **Coffee retro (Noah):** `_ShellBridge` owned a second 1 Hz QTimer polling the SAME cached state `_light_timer` already refreshed on the same cadence; slice 2a unified polling: QML bridge rides `_light_timer.timeout`, no timer of its own (4eb7f14). | `TCT_app/gui/qml_shell.py`, `TCT_app/tct_gui.py` | Noah | 2026-07-11 |
| ANNOYANCE | **Coffee retro (Abel):** estimator constructs a throwaway compiler `BiasStep` purely to feed `_bias_ramp_seconds` shaping fields that already live on `LeafMeta` — pass the two floats directly, decouple cost module from compiler Step types. | `TCT_app/controller/plan_estimate.py` | Abel | 2026-07-11 |
| NIT | **Coffee retro (Abel):** plan_estimate timing defaults (`motor_speed_mm_s=25.0`, `s_per_average=0.02`, `bias_hold_s=1.0`) are magic constants with no link to `configs/devices.yaml` or the executor's real feed/ramp/hold — silent ETA drift with no test to catch it. | `TCT_app/controller/plan_estimate.py` | Abel | 2026-07-11 |
| NIT | **Coffee retro (Noah):** `tct_gui.py` ~1395 lines and growing; extract the QML branch's wiring from `_build_central` into a `gui/qml_shell.py` helper to keep the composition root thin. | `TCT_app/tct_gui.py` | Noah | 2026-07-11 |
| NIT | **Coffee retro (Mamoru):** `ScopeViewModel.update()` covered only indirectly via `_ShellBridge.pull()`; add a dedicated unit test for isolation. | `TCT_app/gui/scope_viewmodel.py` | Noah | 2026-07-11 |
| NIT | yaml_persist tmp-file litter on hard crash + no dir fsync after os.replace. Commit d810c55 uses atomic tmp+fsync+os.replace but does not fsync the parent directory (`configs/`), so a power loss mid-replace could leave both old and new on disk or both corrupt (observed risk on older ext3/NTFS systems). Mary review 2026-07-11: optional hardening, low priority (most modern filesystems auto-journal dir entries). Document or add parent fsync if data integrity risk is deemed unacceptable. | `TCT_app/controller/yaml_persist.py` | Adam/Paul | 2026-07-11 |
| NIT | PlanEstimate lacks public StepCounts/n_moves fields — exact step/move parity assertions blocked (test_plan_parity.py integration needed). Current: only cost/points/runtime exposed; internals not queryable. Useful for hardening the assertion against future refactorings. | `TCT_app/controller/plan_estimate.py`, `TCT_app/tests/test_plan_parity.py` | Abel | 2026-07-11 |
| NOTE | _ScanReaper bounded retention if a scan fn blocks forever. Commits 4d887b4 + 97c07f4: ScanReaper is a GUI-thread-affine strong owner of the scan worker to break ABBA GIL-vs-Qt-mutex deadlock (worker refcount must never reach zero on non-owning thread). If `_run_plan()` blocks forever (user bug or a device hang), the scan worker thread + reaper both persist until the process exits. Accepted by Mary as a consequence of the safety-first deadlock fix (worker teardown is already GUI-thread-safe via explicit join). No active mitigation; if needed, consider a scan-timeout watchdog. | `TCT_app/gui/scan_coordinator.py`, `TCT_app/controller/scan_controller.py` | Abel | 2026-07-11 |
| FOLLOW-UP | 8 latent sibling workers use finished→worker.deleteLater (destroys ~QObject on the worker thread): camera _CameraWorker, laser _LaserWorker, scope _ScopeReader, motor _PositionPoller, bias _ReadoutPoller, intensity _IntensityReader, planner _EstimateWorker, tct_gui LivenessMonitor. Currently SAFE (blocking quit+wait teardown, no counter-party) but same bug class — convert to GUI-side destruction as defense-in-depth; each needs a smoke test + batch Mary review. | `TCT_app/gui/{camera_panel,laser_panel,scope_panel,motor_panel,bias_panel,intensity_panel,planner_panel}.py`, `TCT_app/tct_gui.py` | Noah/Abel | 2026-07-12 |
| NIT | QWidget MetricTile compact variant: `FONT_VALUE_COMPACT_PX` token defined but unwired in `gui/qml/MetricTile.qml` — cosmetic mode for dense layouts (QML follow-up if needed). | `TCT_app/gui/qml/MetricTile.qml`, `TCT_app/gui/style.py` | Noah | 2026-07-12 |
| NIT | Planner ghost-preview offset on empty recipe: drag-drop preview appears offset when the tree is empty or newly opened — cosmetic, low priority. | `TCT_app/gui/planner_panel.py` | Noah | 2026-07-12 |
| NIT | ScanCoordinator dual start-path mutual exclusion: `start_plan()` and `start()` can race if both called concurrently (arm-latch gates one path, but per-action confirm gate still exists on the other); consider an assert or flag to catch concurrent starts. Optional hardening. | `TCT_app/gui/scan_coordinator.py` | Mary/Abel | 2026-07-12 |
| NIT | xdist: full suite runs ~8x faster (-n auto ~25s vs 199s serial) but test_panel_kit_rollout_batch1.py::test_bias_panel_danger_and_rail_hooks_present fails under xdist (passes alone serially) — test-isolation issue; 2nd flaky test seen: test_yaml_tab_card_has_devices_yaml_header; harden before making xdist the bench default. | `TCT_app/tests/test_panel_kit_rollout_batch1.py` | Noah/Mary | 2026-07-12 |
| NIT | QML scan-status slice (Mary review e45496d, all optional): (a) Progress tile concatenates done+"/"+total in the view — could add read-only progressText to RunStateViewModel; (b) ScanStatusStrip fixed height 204 clips a 2nd tile row below ~1280px — compact mode or height-track; (c) null-runState test doesn't assert zero QML console warnings. | `TCT_app/gui/qml/ScanStatusStrip.qml`, `TCT_app/gui/run_state_viewmodel.py`, `TCT_app/tests/test_qml_shell.py` | Noah | 2026-07-12 |
| ANNOYANCE | Sequencer envelope semantics need Kaya ruling (combined vs per-routine) — see `docs/design/feature_requests_v5.md` §7 for context and open questions. | `docs/design/feature_requests_v5.md` | Abel | 2026-07-12 |
| TODO(bench) | Acquire-measurements block needs SCPI manual verification per scope backend — TODO(manual needed): TBS1052C MEASUrement subsystem command forms; DRS4 likely host-side only (no instrument SCPI). Unblock: gather manual citations or confirm via scope panel. | `TCT_app/gui/scope_measurements.py`, `TCT_app/devices/oscilloscope.py` | Paul | 2026-07-12 |
| ANNOYANCE | Slice worktree `slice1-ui` / `origin/qml-hybrid-slice1` is behind `cockpit-v5` — sync or retire after v5 ratification. Staging branch to decide: preserve for feature-branch baseline or prune. | `(branch)` | Adam | 2026-07-12 |
| RESOLVED 2026-07-13 | Mary Track-A A3.1 (motion + pause semantics). Park_safe (7b32dc3) halts all axes + discharges HV; pause gate (88f500f) blocks resume unless PAUSED; pause dialog guard (6e691fd) prevents stray modal; emergency always live (7061e97). Verdict CLOSED. | `TCT_app/controller/scan_controller.py` | Abel | 2026-07-13 |
| RISK | **Kings retro (Abel):** `StateMachine.transition()` is unlocked check-then-act, called from GUI + worker threads — race can mislabel a cleanly-finished run as ABORTED (worker reads RUNNING, GUI sets PAUSED, worker transition(FINISHED) raises → except path lands ABORTED + on_error). Fuzzer only asserts terminality, not the correct label. | `TCT_app/controller/state_machine.py:53` | Abel | 2026-07-12 |
| RISK | **Kings retro (Abel):** slow-control UNAVAILABLE is latched identically to WARN — a dead/unplugged safety sensor safe-holds once, operator acks, then the channel is silently unmonitored for the rest of the run (never reads OK → latch never clears, no re-alert). "Briefly stale" and "permanently gone" need different escalation. | `TCT_app/controller/scan_controller.py:1546-1550` | Abel | 2026-07-12 |
| RISK | **Kings retro (Abel):** armed-envelope expiry unplumbed — planner builds `ArmedEnvelopeGate(env)` with `expiry_monotonic=None` so `is_expired()` is always False; the 10 s arm→Execute freshness rests ONLY on the ArmLatch QTimer (event-loop starvation defeats it). Plumb `timeout_s` for defense in depth. | `TCT_app/gui/planner_panel.py:2248`, `TCT_app/controller/arm_envelope.py:105` | Abel | 2026-07-12 |
| RESOLVED 2026-07-13 | Reference-channel baseline-correction (00d53bc): NEW canonical `analysis/waveform_analysis.correct_baseline()`, mirrored in `intensity_base.py` for ref-channel parity. Guard test ensures byte-for-byte equality. Ref channel now baseline-corrected identically to DUT path. Closes Kings retro item (latent DC-offset bias on all saved charge/CCE values). | `TCT_app/analysis/waveform_analysis.py`, `TCT_app/devices/intensity_base.py`, `TCT_app/devices/intensity_scope_ch.py` | Jonathan | 2026-07-12 |
| RISK | **Kings retro (Jonathan):** `_save_camera_frame` silently `return`s on shape mismatch — no log/counter/attr; later `ds.resize()` zero-backfills the gap, indistinguishable from a real dark frame. Violates "never silently discard data; cuts explicit and counted". | `TCT_app/data/hdf5_writer.py:143-152` | Jonathan | 2026-07-12 |
| RESOLVED 2026-07-13 | V_dep fit-quality (95b27c7 + a3449be): NEW `fit_depletion_voltage()` (2-point fit + quality metrics) + `compute_cce_with_uncertainty()` (ratio + confidence). GUI conditional rendering on fit-quality flag. Closes Kings retro item. | `TCT_app/analysis/efield_analysis.py`, `TCT_app/gui/analysis_panel.py` | Jonathan | 2026-07-12 |
| ANNOYANCE | **Kings retro (Noah):** `scan_map_view.update_point()` → unconditional `_redraw()` rebuilds the FULL grid + setImage on EVERY point (no throttle/debounce) — cost grows with n; multi-thousand-point routines will stutter the GUI thread. DAQ-latency-adjacent: fix before giga-scans. | `TCT_app/gui/scan_map_view.py:254-262,378-438` | Noah | 2026-07-12 |
| PARTIALLY RESOLVED 2026-07-13 | Shared worker-lifecycle primitive (b156cdf): NEW `gui/worker.py` `WorkerThread` (terminal signal to bound method, GUI-thread-safe teardown by construction, no wait-on-self, GUI-side reaping, ShutdownKind.ABANDON vs MUST_COMPLETE). Bias_panel IV-sweep + supply-call workers migrated. ~8 sibling workers (incl. long-lived pollers, may need poller variant) remain, gated behind Mary review. | `TCT_app/gui/worker.py` | Noah | 2026-07-13 |
| ANNOYANCE | **Kings retro (Noah):** QML rail feed reaches past encapsulation — `_collect_shell_state()` reads `chip.property('state')` and `scope_panel._btn_live` (underscore-private); a rename silently blanks the QML readout with zero test failure. Needs accessor seams. | `TCT_app/tct_gui.py:969,974` | Noah | 2026-07-12 |
| NIT | **Kings retro (Noah):** `MeasurementPanel` hardcodes `ACCENT_DARK` ink (wrong in light theme), has NO `refresh_theme()`, still bare QGroupBox instead of panel_kit Card. | `TCT_app/gui/scope_measurements.py:12,146` | Noah | 2026-07-12 |
| NIT | **Kings retro (Noah):** `_YamlHighlighter` bakes 6 GitHub-LIGHT hexes at construction; `refresh_theme()` never rebuilds `self._rules` → light syntax ink on dark editor; its docstring "not currently called by _toggle_theme" is stale (tct_gui.py:824 wires it). | `TCT_app/gui/settings_window.py:75-84,1489-1509,1699` | Noah | 2026-07-12 |
| NIT | **Kings retro (Abel):** state fuzzer only launches via `start_plan` — classic `_run`, `_run_z_focus`, `_run_voltage_scan` loops (the most HV-active!) get zero randomized pause/resume/abort/fault interleaving. Extend `_START_FLAVORS`. | `TCT_app/tests/test_state_fuzz.py:178,227` | Abel | 2026-07-12 |
| NIT | **Kings retro (Jonathan):** `reconstruct_efield`/`silicon_mobility` have zero test coverage (currently unwired dead code — the landmine ships the day the E-field feature wires them). | `TCT_app/analysis/efield_analysis.py:97-148` | Jonathan | 2026-07-12 |
| NIT | **Kings retro (Jonathan):** map colorbar renders doubled units ("dut_charge_pC (pC)") — exact trap `map_slice.strip_unit_suffix()` solved tonight, never backported to the adjacent map view. | `TCT_app/gui/scan_map_view.py:441` | Jonathan | 2026-07-12 |
| ANNOYANCE | **Kings retro (Samantha):** docs lag the cockpit — ARCHITECTURE.md module index missing gui/theme_editor.py + gui/motion.py; BENCH_CHECKLIST §5a/6a describe the pre-latch HV/start flow (operator-facing!); README has no QML shell/-Qml/MultiBiasPanel; run.ps1 comment about to invert unwarned; docs/design/ (19 files) lacks an index + supersession markers. Fix beat tonight. | `docs/ARCHITECTURE.md`, `docs/BENCH_CHECKLIST.md`, `TCT_app/README.md`, `TCT_app/run.ps1`, `docs/design/` | Samantha | 2026-07-12 |
| NOTE | **Kings retro (Mary, process — adopted):** reviews must not stack committed+uncommitted or multiple features in one batch; one feature = one verdict. Slicer review dispatched standalone; theme editor gets its own. In-flight fixes tonight: coordinator sibling slots (Mary#1), driver-truth batch (Paul#1-4). | `(process)` | Adam | 2026-07-12 |
| RISK | **Monkey harness find (Abel D2):** `gui/scope_panel.py:1010` `_on_avg_changed` does `self._scope.n_averages = n` on a READ-ONLY property → unhandled AttributeError on EVERY Averages-combo change (user-reachable). Line 1013 then calls `_save_scope_keys_to_yaml()` — the fix must not start persisting a value it failed to apply. Pinned as strict-xfail in tests/test_ui_monkey.py (goes RED when fixed → delete the quarantine entry). | `TCT_app/gui/scope_panel.py:1010` | Noah | 2026-07-12 |
| RESOLVED 2026-07-13 | flash_button unowned timer (2d4684b): `gui/status_widgets.py:365` already uses context-object overload `QTimer.singleShot(timeout_ms, button, _restore)` — Qt cancels pending invocation on button destroy. Closes "timer fires on deleted C++ object" class + test-suite inter-test pollution. | `TCT_app/gui/status_widgets.py:365` | Noah | 2026-07-12 |
| RISK | **Data bug (Jonathan, confirmed):** `gui/scan_map_view.QUANTITIES` lists `drift_time_s`/`rise_time_s`/`cfd_time_s` (ScanResult attribute names, seconds — used by the live-stream path via getattr), but `hdf5_writer.save_point` writes those three as `*_ns` (×1e9). AnalysisPanel reads HDF5 keys 1:1, so 3 of 8 selectable quantities can NEVER be found on a loaded run; `onset_time_ns` is written but has no QUANTITIES entry at all. Live path and loaded path disagree on names AND units. | `TCT_app/gui/scan_map_view.py:66-91`, `TCT_app/data/hdf5_writer.py` | Jonathan | 2026-07-12 |
| RISK | **Design-law / safety-affordance (Abel D2):** the Z-focus "Find focus" button carries `state="primary"` — NOT motion/danger — yet starts a real motion+acquisition run. Widget role-markings alone are not a sufficient danger filter (the monkey's allowlist therefore also denies by verb). W1 taxonomy sweep must give it the motion class. | `TCT_app/gui/scan_viewer_panel.py` | Noah | 2026-07-12 |
| NIT | **Codex D5 review:** `analysis/map_slice.mm_to_index()` has no non-finite guard — a NaN cut position makes `argmin(abs(axis - nan))` return index 0, silently slicing the first row/column instead of refusing. GUI spin prevents it today, but the analysis helper is public. | `TCT_app/analysis/map_slice.py:87-90` | Jonathan | 2026-07-12 |
| NIT | **Codex D5 review:** `style.load_theme_customization()` leaves `_glass_amount` unchanged when `theme/glass_amount` is absent and defaults radius to the CURRENT `_radius_scale` (not "m") — an in-process profile switch can inherit the previous profile's glass/radius. Cold start masks it. | `TCT_app/gui/style.py:1684-1708` | Noah | 2026-07-12 |
| NOTE | **Codex D5 verdict (locked tokens):** traced — NO override path reaches `danger/armed/sim/error` (+`crit`/`warn` aliases): SAFETY_TOKENS locks them, the editable fan-out is accent/canvas/panel/well/text/muted/hairline only, `apply_theme_overrides` raises on safety keys, and both QSettings + preset loads pass through `sanitize_overrides`. Glass recompute writes only chrome/strip/edge/edge_shade. Residual (design, not safety): `accent` can still be set red/green-ish, weakening hierarchy around the locked tokens. | `TCT_app/gui/style.py`, `TCT_app/gui/theme_editor.py` | — | 2026-07-12 |
| RESOLVED 2026-07-13 | **API footgun (034c176):** `BiasChannel.output_on()` renamed to `enable_output()`; read-only `is_output_on` property added. Guard test `test_bias_api_guard.py` prevents truthiness trap from being reintroduced. | `TCT_app/devices/bias_supply_base.py` | Paul | 2026-07-13 |
| RESOLVED 2026-07-13 | **Terminal-state race (26bcf95):** `StateMachine.transition()` now protected by RLock (check-then-act atomic); callbacks invoked outside lock. ValueError-on-race is documented contract. Eliminates the reported mislabel under xdist contention. | `TCT_app/controller/state_machine.py` | Abel | 2026-07-13 |
| RISK | **QSettings isolation is a conftest shim, not structural (Noah):** `QSettings("TCT","TCTSetup")` is named in ~6 app sites; Qt ignores `setDefaultFormat` for the (org,app) ctor and `setPath` for NativeFormat on Windows, so the test suite wrote into the DEVELOPER'S REAL registry (theme, geometry, detached tabs) until 2d4684b patched it at test level. Fix structurally: one `gui/app_settings.py` accessor the whole app goes through. | `TCT_app/gui/*.py`, `TCT_app/tests/conftest.py` | Noah | 2026-07-13 |
| NIT | `pytest-xdist` is installed on the BENCH venv only, not in `TCT_app/requirements.txt` — `-n auto` therefore does not work on the laptop. Add it if local parallel runs are ever wanted (bench policy says heavy suites go to sophonone anyway). | `TCT_app/requirements.txt` | Adam | 2026-07-13 |
| ANNOYANCE | WaveformGenerator.output_on() has the same truthiness-trap shape as the defused BiasChannel one — no HV stakes, but consider applying the same enable_output() + is_output_on rename for consistency. | `TCT_app/devices/waveform_generator.py` | Paul | 2026-07-13 |
| ANNOYANCE | docs/TCT_app_overview.ipynb:207+213 stale: shows `def output_on` code snippet and claims BiasReading has an output_on field (it does not — actual fields: voltage_V/current_A/compliant/status_word/tripped/output_on_hw); notebook needs a refresh pass post-rename. | `docs/TCT_app_overview.ipynb` | Jonathan | 2026-07-13 |
| RESOLVED 2026-07-13 | gui/bias_panel.py:855 finished→setEnabled lambda (b156cdf): IV-sweep worker migration to `WorkerThread` primitive; terminal signal to bound method (setEnabled) routes teardown to GUI thread. GUI-side reaping eliminates the race. | `TCT_app/gui/bias_panel.py` | Noah | 2026-07-13 |
| RESOLVED 2026-07-13 | Theme editor now self-applies backdrop/opacity at construction (d100650). User sees material effects immediately on launch without requiring manual Apply click. | `TCT_app/gui/settings_window.py`, `TCT_app/gui/theme_editor.py` | Noah | 2026-07-13 |
| NIT | Motor utility buttons (Test connection / Use current position / Set as scan start) stay live during sequence — bounded GUI-stall on GRBL mutex; optional one-line lock for UX coherence (owner Noah). Non-blocking; lower priority. | `TCT_app/gui/motor_panel.py` | Noah | 2026-07-13 |
| ANNOYANCE | ScanPlan has no generic metadata field — survey geometry rides namespaced in `safety['survey']` (E6a); a real `plan.metadata` is cleaner (owner Abel). Queued for data-schema refinement. | `TCT_app/controller/scan_plan.py` | Abel | 2026-07-13 |
| ANNOYANCE | Writer-side per-frame stage-position record: `camera/frame_pos_mm` queued as the E6b data-lane decision (owner Jonathan). Affects `data/hdf5_writer.py` and `SCAN_DATA_FORMAT.md`. | `TCT_app/data/hdf5_writer.py` | Jonathan | 2026-07-13 |
| ANNOYANCE | `SCAN_DATA_FORMAT.md` still lacks a CAPTURE_PHOTO/save_camera_frame subsection (owner Jonathan, prose Samantha). Prose docs follow the data contract. | `TCT_app/SCAN_DATA_FORMAT.md` | Jonathan/Samantha | 2026-07-13 |
| RISK | Sequencer PreflightHook raise-handling: unhandled exception path + "unknown" outcome word if hook fails. park_safe() is primary-channel-only (secondary channels left charged). Mary Track-A REQUEST-CHANGES (A3.1 fix beat queued). | `TCT_app/controller/sequencer.py`, `TCT_app/controller/scan_controller.py` | Abel | 2026-07-13 |
| ANNOYANCE | E7c overlay places markers with nominal isotropic px/mm even when a rotation/shear affine is on file — pose NUMBERS are exact, only the diagnostic overlay is approximated (89c624a). | `TCT_app/gui/analysis_panel.py` | Jonathan/Noah | 2026-07-13 |
| ANNOYANCE | E7c reference-frame choice is frame-0-vs-latest within-run drift heuristic; operator-selectable stored reference = future beat. | `TCT_app/gui/analysis_panel.py` | Jonathan | 2026-07-13 |
| NIT | RADIUS["xl"] (54bc4b8) has no QML counterpart in qml_theme.py (radiusSm/Md/Lg/Pill only) — gap if QML chrome ever draws card radii. | `TCT_app/gui/qml_theme.py` | Noah | 2026-07-13 |
| RISK | calibrate_affine lacks a singularity/scale sanity check — zero-motion textured frames passing the quality gate yield a finite near-singular fit with no raise (E4 gap: degenerate handling returns None but no pre-check on motion magnitude). | `TCT_app/controller/repeatability.py` | Paul | 2026-07-13 |
| RISK | metrology_report.py rotation/shear decomposition is a local convention (E5 implementation detail) — needs verification against real-world camera mounts before quantitative reuse outside the pass/fail gate. | `TCT_app/scripts/metrology_report.py` | Jonathan | 2026-07-13 |
| NIT | controller/survey_plan.py module docstring still says the E6b frame-position gap is open ("KNOWN GAP … queued for E6b") — stale since 95ac0f0/50961c7 landed per-frame stage positions. Trivial docstring update. | `TCT_app/controller/survey_plan.py` | Abel or Samantha | 2026-07-13 |
| RISK | hdf5_writer.set_camera_calibration has NO call site — `calibrate_affine` results are never persisted to HDF5; the Survey view reads the stored calibration that nothing writes yet (E6c gap). | `TCT_app/data/hdf5_writer.py` | Jonathan/Abel | 2026-07-13 |
| ANNOYANCE | gui/style.py theme/* persistence still constructs QSettings directly, allowlisted in test_app_settings.py — migrate onto gui/app_settings central accessor (S2c rider, low priority). | `TCT_app/gui/style.py` | Noah/Codex | 2026-07-13 |
| ANNOYANCE | scan_map_view flush-on-cursor-readout partially bypasses the new redraw throttle while hovering during a live scan (S2b: 31ed97b throttles unconditional update_point redraws; readout accessors flush immediately for latency-conscious feedback). Deliberate correctness-first choice; revisit after Mary review for UX tuning. | `TCT_app/gui/scan_map_view.py` | Noah | 2026-07-13 |
