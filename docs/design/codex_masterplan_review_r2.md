# Codex Masterplan Review R2

Scope: only the `Codex-R1 corrections` block plus the Bounce-5 ledger entry,
checked against nearby roadmap text for lost meaning or new contradictions.

## Per-Finding Check

- BLOCKER-1 - FAITHFUL. Lifecycle and transport reservation capture the
  amendment and fit Bounce-1 F1 because they live on `CapabilityBinding`, not
  descriptor data. Residual: the code sketch still shows `setter/getter` on
  `SweepableParameter`; edit it or mark it illustrative.
- BLOCKER-2 - FAITHFUL. The event-authority gate includes key injection, mouse
  hit tests, z-order assertions, and top-level QWidget emergency shortcuts. No
  new QML contradiction found.
- MAJOR-1 - WEAKENED. The technical fix is faithful, but the flag frames
  per-operation routing as a Kaya choice. It is a forced safety correction;
  Kaya can ratify taxonomy/Volundr compatibility, not the old single ladder.
- MAJOR-2 - WEAKENED. The corrections block requires a per-point command trace
  and `swept/` comparison, but the Planner section still says P0' writes
  wavegen settings into `run_metadata`. Edit it so P0' cannot become run-level
  metadata again.
- MAJOR-3 - FAITHFUL. Timing/status plus an atomic completion marker preserve
  the amendment and can coexist with the later outcome/abort seed contract.
- MAJOR-4 - FAITHFUL. Namespaced shell keys, frozen shared keys in
  `app_settings`, and dirty classic/qml/classic round trip preserve the gate.
- MAJOR-5 - CONTRADICTS. The corrections re-rate PORT1 as M/L, but Portability
  still labels `PORT1 (S/M)`. Make all PORT1 labels M/L and carry the graphics
  artifacts into the standing gate text.
- MINOR-1 - CONTRADICTS. The corrections split D1 into D1a/D1b and rate D4 as
  L, but Devices still lists D1 capability layer + adapters as M, D4
  composition as M, and first beats still combine model and adapters.
- MINOR-2 - WEAKENED. The conditional seed-cleanliness rule is correct, but
  Devices still says e4control license status is "RESOLVED by Kaya". Public
  seed has one sound option: grant/license by tag, or private appendix.

## Flagged Items

Per-operation routing needs Kaya re-ratification because it touches Volundr's
total-ordering decision, but the safe outcome is not optional. The e4control
item is also forced for public seed cleanliness; Kaya chooses permission vs
private appendix, not whether unlicensed derived seed material can ship.

## Final Verdict

Not ready as integrated. One text cleanup pass should reconcile the binding
sketch, Planner P0' wording, PORT1 labels, D1/D4 effort split, and e4control
"resolved" language. After that, R1 substance is preserved and R2 adds no
app-code work.
