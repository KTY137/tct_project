RISK 2 banding proof (Mary's pilot review).
before_fullres: the full (pre-cap) bucketed wash drawn into 2560x1440.
after_capped:   ground_pixmap() capped to 1024px long edge, then drawPixmap-scaled to the same size (the shipped path).
max per-pixel deltaL* between them: 0.7231, a single-pixel peak at the toplight's steepest edge; below the ~1.0 JND threshold, so no
visible banding (the wash's whole span is only deltaL* ~3.58).
